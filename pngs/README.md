This directory is mounted as `/assets`.

The production deployment can place optional public images here, for example:

- `contact-qr.png` for a manual refund contact QR code
- `refund-sample.png` for a refund receipt example

Do not commit personal QR codes, payment receipts, private screenshots, or any other sensitive image assets.
